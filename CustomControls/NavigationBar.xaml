<UserControl x:Class="PuzzleRpg.CustomControls.NavigationBar"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    FontFamily="{StaticResource PhoneFontFamilyNormal}"
    FontSize="{StaticResource PhoneFontSizeNormal}"
    Foreground="{StaticResource PhoneForegroundBrush}"
    d:DesignHeight="100" d:DesignWidth="480">

    <Grid x:Name="LayoutRoot" Background="{StaticResource PhoneChromeBrush}">
        <Rectangle Grid.Column="1" x:Name="FirstNavigationItem" Height="100" Width="100" Fill="AliceBlue" Tap="FirstNavigationItem_Tap" HorizontalAlignment="Left"/>
        <Rectangle Grid.Column="2" x:Name="SecondNavigationItem" Height="100" Width="100" Margin="135,0,245,0" Fill="Blue" Tag="SecondNavigationItem_Tap" />
        <Rectangle Grid.Column="3" x:Name="ThirdNavigationItem" Height="100" Width="100" Margin="345,0,35,0" Fill="Yellow" Tag="ThirdNavigationItem_Tap" />
        <Rectangle Grid.Column="4" x:Name="FourthNavigationItem" Height="100" Width="100" Margin="240,0,140,0" Fill="Green" Tag="FourthNavigationItem_Tap" />
    </Grid>
</UserControl>